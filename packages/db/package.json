{
  "name": "@packages/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo \"No build step required for @packages/db\"",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.1"
  }
}
